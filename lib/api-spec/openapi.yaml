openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 1.0.0
  description: Autonomous YouTube Factory API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: projects
    description: Video project management
  - name: pipelines
    description: Pipeline execution and monitoring
  - name: jobs
    description: Background job management
  - name: services
    description: AI service execution
  - name: analytics
    description: Channel and video analytics
  - name: settings
    description: Application configuration
  - name: logs
    description: System log access
  - name: research
    description: AI research service
  - name: scripts
    description: AI script generation service
  - name: assets
    description: Asset Intelligence Engine — image/video/icon acquisition

paths:
  # ── Research ──────────────────────────────────────────────────────────────
  /research:
    get:
      operationId: listResearch
      tags: [research]
      summary: List research results
      parameters:
        - name: status
          in: query
          schema:
            type: ["string", "null"]
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
        - name: offset
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Research list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchList"
    post:
      operationId: startResearch
      tags: [research]
      summary: Start an async research job
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResearchInput"
      responses:
        "202":
          description: Research job started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchResult"

  /research/{id}:
    get:
      operationId: getResearch
      tags: [research]
      summary: Get research result by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Research result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResearchResult"
        "404":
          description: Not found
    delete:
      operationId: deleteResearch
      tags: [research]
      summary: Delete a research result
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  # ── Storyboards ────────────────────────────────────────────────────────────
  /storyboards:
    get:
      operationId: listStoryboards
      tags: [storyboards]
      summary: List storyboard results
      parameters:
        - name: status
          in: query
          schema:
            type: ["string", "null"]
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
        - name: offset
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Storyboard list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryboardList"
    post:
      operationId: startStoryboard
      tags: [storyboards]
      summary: Start an async storyboard-generation job
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StoryboardInput"
      responses:
        "202":
          description: Storyboard job accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryboardResult"

  /storyboards/{id}:
    get:
      operationId: getStoryboard
      tags: [storyboards]
      summary: Get storyboard result by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Storyboard result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StoryboardResult"
        "404":
          description: Not found
    delete:
      operationId: deleteStoryboard
      tags: [storyboards]
      summary: Delete a storyboard result
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  # ── Assets ────────────────────────────────────────────────────────────────
  /assets:
    get:
      operationId: listAssets
      tags: [assets]
      summary: List asset results
      parameters:
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
        - name: offset
          in: query
          schema:
            type: ["integer", "null"]
        - name: storyboardId
          in: query
          schema:
            type: ["string", "null"]
        - name: sceneId
          in: query
          schema:
            type: ["string", "null"]
        - name: status
          in: query
          schema:
            type: ["string", "null"]
        - name: assetKind
          in: query
          schema:
            type: ["string", "null"]
        - name: provider
          in: query
          schema:
            type: ["string", "null"]
      responses:
        "200":
          description: Asset list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetList"
    post:
      operationId: acquireAssets
      tags: [assets]
      summary: Enqueue asset acquisition for a storyboard
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInput"
      responses:
        "202":
          description: Asset acquisition queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAcquireResponse"

  /assets/{id}:
    get:
      operationId: getAsset
      tags: [assets]
      summary: Get asset result by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Asset result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetResult"
        "404":
          description: Not found
    delete:
      operationId: deleteAsset
      tags: [assets]
      summary: Delete an asset result
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /assets/providers:
    get:
      operationId: listAssetProviders
      tags: [assets]
      summary: List asset provider statistics
      responses:
        "200":
          description: Provider statistics list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetProviderStatsList"

  # ── Scripts ───────────────────────────────────────────────────────────────
  /scripts:
    get:
      operationId: listScripts
      tags: [scripts]
      summary: List script results
      parameters:
        - name: status
          in: query
          schema:
            type: ["string", "null"]
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
        - name: offset
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Script list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScriptList"
    post:
      operationId: startScript
      tags: [scripts]
      summary: Start an async script-generation job
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ScriptInput"
      responses:
        "202":
          description: Script job started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScriptResult"

  /scripts/{id}:
    get:
      operationId: getScript
      tags: [scripts]
      summary: Get script result by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Script result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScriptResult"
        "404":
          description: Not found
    delete:
      operationId: deleteScript
      tags: [scripts]
      summary: Delete a script result
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ── Projects ──────────────────────────────────────────────────────────────
  /projects:
    get:
      operationId: listProjects
      tags: [projects]
      summary: List all projects
      parameters:
        - name: status
          in: query
          schema:
            type: ["string", "null"]
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
        - name: offset
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Project list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectList"
    post:
      operationId: createProject
      tags: [projects]
      summary: Create a new project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectInput"
      responses:
        "201":
          description: Created project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"

  /projects/{id}:
    get:
      operationId: getProject
      tags: [projects]
      summary: Get project by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Not found
    patch:
      operationId: updateProject
      tags: [projects]
      summary: Update project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectUpdate"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
    delete:
      operationId: deleteProject
      tags: [projects]
      summary: Delete project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted

  /projects/{id}/run:
    post:
      operationId: runProject
      tags: [projects]
      summary: Start the full pipeline for a project
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "202":
          description: Pipeline started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineRun"

  # ── Pipelines ─────────────────────────────────────────────────────────────
  /pipelines:
    get:
      operationId: listPipelines
      tags: [pipelines]
      summary: List pipeline runs
      parameters:
        - name: project_id
          in: query
          schema:
            type: ["string", "null"]
        - name: status
          in: query
          schema:
            type: ["string", "null"]
      responses:
        "200":
          description: Pipeline list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineList"

  /pipelines/{id}:
    get:
      operationId: getPipeline
      tags: [pipelines]
      summary: Get pipeline run details and stage progress
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Pipeline run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineRun"

  /pipelines/{id}/cancel:
    post:
      operationId: cancelPipeline
      tags: [pipelines]
      summary: Cancel a running pipeline
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineRun"

  /pipelines/{id}/retry:
    post:
      operationId: retryPipeline
      tags: [pipelines]
      summary: Retry a failed pipeline
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "202":
          description: Retried
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineRun"

  # ── Jobs ──────────────────────────────────────────────────────────────────
  /jobs:
    get:
      operationId: listJobs
      tags: [jobs]
      summary: List background jobs
      parameters:
        - name: status
          in: query
          schema:
            type: ["string", "null"]
        - name: type
          in: query
          schema:
            type: ["string", "null"]
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Job list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobList"

  /jobs/{id}:
    get:
      operationId: getJob
      tags: [jobs]
      summary: Get job details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"

  /jobs/{id}/retry:
    post:
      operationId: retryJob
      tags: [jobs]
      summary: Retry a failed job
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "202":
          description: Retried
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"

  /jobs/{id}/cancel:
    post:
      operationId: cancelJob
      tags: [jobs]
      summary: Cancel a pending or running job
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Job"

  # ── Dashboard / Analytics ─────────────────────────────────────────────────
  /analytics/dashboard:
    get:
      operationId: getDashboardStats
      tags: [analytics]
      summary: Aggregate dashboard statistics
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /analytics/pipeline-activity:
    get:
      operationId: getPipelineActivity
      tags: [analytics]
      summary: Recent pipeline activity feed
      parameters:
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Activity feed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityFeed"

  /analytics/stage-breakdown:
    get:
      operationId: getStageBreakdown
      tags: [analytics]
      summary: Pipeline stage success/failure breakdown
      responses:
        "200":
          description: Stage breakdown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StageBreakdown"

  # ── Logs ──────────────────────────────────────────────────────────────────
  /logs:
    get:
      operationId: listLogs
      tags: [logs]
      summary: List system log entries
      parameters:
        - name: level
          in: query
          schema:
            type: ["string", "null"]
        - name: service
          in: query
          schema:
            type: ["string", "null"]
        - name: limit
          in: query
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Log entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogList"

  # ── Settings ──────────────────────────────────────────────────────────────
  /settings:
    get:
      operationId: getSettings
      tags: [settings]
      summary: Get application settings
      responses:
        "200":
          description: Settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"
    patch:
      operationId: updateSettings
      tags: [settings]
      summary: Update application settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SettingsUpdate"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    # ── Research ─────────────────────────────────────────────────────────────
    ResearchInput:
      type: object
      required: [topic]
      properties:
        topic:             { type: string }
        targetAudience:    { type: string }
        videoLengthMinutes: { type: integer }
        language:          { type: string }
        style:             { type: string }
        tone:              { type: string }
        providers:
          type: array
          items: { type: string }

    ResearchKeyword:
      type: object
      required: [term, relevance]
      properties:
        term:         { type: string }
        relevance:    { type: number }
        searchVolume: { type: ["integer", "null"] }
        difficulty:   { type: ["string", "null"] }
        semanticTags:
          type: array
          items: { type: string }

    ResearchReference:
      type: object
      required: [title, url, sourceType, credibilityScore, citationFormat]
      properties:
        id:               { type: string }
        title:            { type: string }
        url:              { type: string }
        sourceType:       { type: string }
        author:           { type: ["string", "null"] }
        publishedAt:      { type: ["string", "null"] }
        snippet:          { type: ["string", "null"] }
        credibilityScore: { type: number }
        citationFormat:   { type: string }
        provider:         { type: string }

    ResearchSection:
      type: object
      required: [sectionType, title, content, confidence]
      properties:
        sectionType:  { type: string }
        title:        { type: string }
        content:      { type: string }
        confidence:   { type: number }
        items:
          type: array
          items: { type: string }
        sourceIds:
          type: array
          items: { type: string }

    ResearchResult:
      type: object
      required: [id, topic, status, providers, usedProviders, sections, references, keywords, logs, createdAt, updatedAt]
      properties:
        id:                  { type: string }
        topic:               { type: string }
        targetAudience:      { type: ["string", "null"] }
        videoLengthMinutes:  { type: integer }
        language:            { type: string }
        style:               { type: string }
        tone:                { type: string }
        status:
          type: string
          enum: [pending, running, completed, failed, cached]
        jobId:               { type: ["string", "null"] }
        summary:             { type: ["string", "null"] }
        confidenceScore:     { type: ["number", "null"] }
        estimatedDifficulty: { type: ["string", "null"] }
        sections:
          type: array
          items: { $ref: "#/components/schemas/ResearchSection" }
        references:
          type: array
          items: { $ref: "#/components/schemas/ResearchReference" }
        keywords:
          type: array
          items: { $ref: "#/components/schemas/ResearchKeyword" }
        providers:
          type: array
          items: { type: string }
        usedProviders:
          type: array
          items: { type: string }
        logs:
          type: array
          items: { type: string }
        errorMessage:  { type: ["string", "null"] }
        createdAt:     { type: string }
        updatedAt:     { type: string }
        completedAt:   { type: ["string", "null"] }

    ResearchList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ResearchResult" }
        total: { type: integer }

    # ── Project ──────────────────────────────────────────────────────────────
    Project:
      type: object
      required: [id, title, status, topic, createdAt, updatedAt]
      properties:
        id:          { type: string }
        title:       { type: string }
        topic:       { type: string }
        description: { type: ["string", "null"] }
        status:
          type: string
          enum: [draft, queued, running, completed, failed, cancelled]
        thumbnailUrl:  { type: ["string", "null"] }
        videoUrl:      { type: ["string", "null"] }
        youtubeId:     { type: ["string", "null"] }
        youtubeUrl:    { type: ["string", "null"] }
        pipelineId:    { type: ["string", "null"] }
        tags:
          type: array
          items: { type: string }
        createdAt:   { type: string }
        updatedAt:   { type: string }

    ProjectInput:
      type: object
      required: [title, topic]
      properties:
        title:       { type: string }
        topic:       { type: string }
        description: { type: string }
        tags:
          type: array
          items: { type: string }

    ProjectUpdate:
      type: object
      properties:
        title:       { type: string }
        topic:       { type: string }
        description: { type: string }
        status:      { type: string }
        tags:
          type: array
          items: { type: string }

    ProjectList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/Project" }
        total: { type: integer }

    # ── Pipeline ─────────────────────────────────────────────────────────────
    PipelineRun:
      type: object
      required: [id, projectId, status, stages, startedAt]
      properties:
        id:          { type: string }
        projectId:   { type: string }
        status:
          type: string
          enum: [queued, running, completed, failed, cancelled]
        currentStage: { type: ["string", "null"] }
        progress:     { type: integer }
        stages:
          type: array
          items: { $ref: "#/components/schemas/PipelineStage" }
        startedAt:    { type: string }
        completedAt:  { type: ["string", "null"] }
        errorMessage: { type: ["string", "null"] }

    PipelineStage:
      type: object
      required: [name, status, order]
      properties:
        name:       { type: string }
        status:
          type: string
          enum: [pending, running, completed, failed, skipped]
        order:      { type: integer }
        startedAt:  { type: ["string", "null"] }
        completedAt: { type: ["string", "null"] }
        durationMs: { type: ["integer", "null"] }
        error:      { type: ["string", "null"] }

    PipelineList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/PipelineRun" }
        total: { type: integer }

    # ── Job ──────────────────────────────────────────────────────────────────
    Job:
      type: object
      required: [id, type, status, createdAt]
      properties:
        id:          { type: string }
        type:        { type: string }
        status:
          type: string
          enum: [pending, running, completed, failed, cancelled, retrying]
        projectId:   { type: ["string", "null"] }
        pipelineId:  { type: ["string", "null"] }
        payload:
          type: object
          additionalProperties: {}
        result:
          type: ["object", "null"]
          additionalProperties: {}
        error:       { type: ["string", "null"] }
        retryCount:  { type: integer }
        maxRetries:  { type: integer }
        createdAt:   { type: string }
        startedAt:   { type: ["string", "null"] }
        completedAt: { type: ["string", "null"] }

    JobList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/Job" }
        total: { type: integer }

    # ── Analytics ────────────────────────────────────────────────────────────
    DashboardStats:
      type: object
      required: [totalProjects, totalVideos, activeJobs, queuedJobs, successRate, totalRuntime]
      properties:
        totalProjects: { type: integer }
        totalVideos:   { type: integer }
        activeJobs:    { type: integer }
        queuedJobs:    { type: integer }
        failedJobs:    { type: integer }
        successRate:   { type: number }
        totalRuntime:  { type: integer }
        projectsByStatus:
          type: array
          items: { $ref: "#/components/schemas/StatusCount" }
        recentActivity:
          type: array
          items: { $ref: "#/components/schemas/ActivityItem" }

    StatusCount:
      type: object
      required: [status, count]
      properties:
        status: { type: string }
        count:  { type: integer }

    ActivityItem:
      type: object
      required: [id, type, message, timestamp]
      properties:
        id:        { type: string }
        type:      { type: string }
        message:   { type: string }
        projectId: { type: ["string", "null"] }
        status:    { type: ["string", "null"] }
        timestamp: { type: string }

    ActivityFeed:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ActivityItem" }

    StageBreakdown:
      type: object
      required: [stages]
      properties:
        stages:
          type: array
          items: { $ref: "#/components/schemas/StageStats" }

    StageStats:
      type: object
      required: [name, completed, failed, avgDurationMs]
      properties:
        name:          { type: string }
        completed:     { type: integer }
        failed:        { type: integer }
        avgDurationMs: { type: integer }

    # ── Logs ─────────────────────────────────────────────────────────────────
    LogEntry:
      type: object
      required: [id, level, message, service, timestamp]
      properties:
        id:        { type: string }
        level:
          type: string
          enum: [debug, info, warn, error]
        message:   { type: string }
        service:   { type: string }
        projectId: { type: ["string", "null"] }
        jobId:     { type: ["string", "null"] }
        meta:
          type: ["object", "null"]
          additionalProperties: {}
        timestamp: { type: string }

    LogList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/LogEntry" }
        total: { type: integer }

    # ── Settings ─────────────────────────────────────────────────────────────
    Settings:
      type: object
      required: [id, youtubeEnabled, autoUpload, defaultLanguage, maxConcurrentJobs, updatedAt]
      properties:
        id:                  { type: string }
        youtubeEnabled:      { type: boolean }
        autoUpload:          { type: boolean }
        defaultLanguage:     { type: string }
        maxConcurrentJobs:   { type: integer }
        openaiModel:         { type: string }
        imageProvider:       { type: string }
        voiceProvider:       { type: string }
        defaultVideoQuality: { type: string }
        notificationsEmail:  { type: ["string", "null"] }
        webhookUrl:          { type: ["string", "null"] }
        updatedAt:           { type: string }

    SettingsUpdate:
      type: object
      properties:
        youtubeEnabled:      { type: boolean }
        autoUpload:          { type: boolean }
        defaultLanguage:     { type: string }
        maxConcurrentJobs:   { type: integer }
        openaiModel:         { type: string }
        imageProvider:       { type: string }
        voiceProvider:       { type: string }
        defaultVideoQuality: { type: string }
        notificationsEmail:  { type: string }
        webhookUrl:          { type: string }

    # ── Scripts ───────────────────────────────────────────────────────────────
    ScriptInput:
      type: object
      required: [topic]
      properties:
        researchId:            { type: ["string", "null"] }
        topic:                 { type: string, minLength: 3, maxLength: 500 }
        style:
          type: string
          enum: [educational, documentary, storytelling, tutorial, news, long_form, shorts]
        tone:
          type: string
          enum: [engaging, authoritative, casual, inspirational, conversational]
        language:              { type: string }
        targetAudience:        { type: string }
        targetDurationMinutes: { type: integer, minimum: 1, maximum: 120 }
        providers:
          type: array
          items: { type: string }

    ScriptSection:
      type: object
      required: [sectionType, title, content]
      properties:
        sectionType:        { type: string }
        title:              { type: string }
        content:            { type: string }
        wordCount:          { type: integer }
        durationSeconds:    { type: number }
        order:              { type: integer }
        transitionIn:       { type: ["string", "null"] }
        transitionOut:      { type: ["string", "null"] }
        storytellingNotes:  { type: ["string", "null"] }
        visualSuggestion:   { type: ["string", "null"] }

    ScriptResult:
      type: object
      required: [id, topic, status, providers, usedProviders, sections, logs, createdAt, updatedAt]
      properties:
        id:                      { type: string }
        researchId:              { type: ["string", "null"] }
        topic:                   { type: string }
        title:                   { type: ["string", "null"] }
        status:
          type: string
          enum: [pending, running, completed, failed, cached]
        style:                   { type: string }
        tone:                    { type: string }
        language:                { type: string }
        targetAudience:          { type: string }
        targetDurationMinutes:   { type: integer }
        version:                 { type: integer }
        hook:                    { type: ["string", "null"] }
        introduction:            { type: ["string", "null"] }
        outro:                   { type: ["string", "null"] }
        callToAction:            { type: ["string", "null"] }
        sections:
          type: array
          items: { $ref: "#/components/schemas/ScriptSection" }
        wordCount:               { type: ["integer", "null"] }
        estimatedDurationSeconds: { type: ["integer", "null"] }
        readingTimeSeconds:      { type: ["integer", "null"] }
        sceneCount:              { type: ["integer", "null"] }
        pacingWpm:               { type: ["number", "null"] }
        narrationTiming:
          type: array
          items: { type: object }
        emphasisMarkers:
          type: array
          items: { type: object }
        pauses:
          type: array
          items: { type: object }
        pronunciationHints:
          type: array
          items: { type: object }
        visualCues:
          type: array
          items: { type: object }
        versions:
          type: array
          items: { type: object }
        providers:
          type: array
          items: { type: string }
        usedProviders:
          type: array
          items: { type: string }
        jobId:          { type: ["string", "null"] }
        logs:
          type: array
          items: { type: string }
        errorMessage:   { type: ["string", "null"] }
        createdAt:      { type: string }
        updatedAt:      { type: string }
        completedAt:    { type: ["string", "null"] }

    ScriptList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ScriptResult" }
        total: { type: integer }

    # ── Storyboards ────────────────────────────────────────────────────────────
    StoryboardScene:
      type: object
      required: [scene_number, scene_title, start_time_ms, end_time_ms, duration_ms, narration, visual_description, prompts]
      properties:
        scene_number:              { type: integer }
        scene_title:               { type: string }
        start_time_ms:             { type: integer }
        end_time_ms:               { type: integer }
        duration_ms:               { type: integer }
        narration:                 { type: string }
        visual_description:        { type: string }
        visual_type:               { type: string }
        prompts:                   { type: object }
        shot_type:                 { type: string }
        camera_angle:              { type: string }
        camera_movement:           { type: string }
        zoom_instructions:         { type: ["string", "null"] }
        pan_instructions:          { type: ["string", "null"] }
        animation_suggestions:     { type: array, items: { type: object } }
        transition_type:           { type: string }
        transition_duration_ms:    { type: integer }
        scene_emotion:             { type: string }
        color_palette:             { type: array, items: { type: string } }
        lighting_style:            { type: string }
        background_description:    { type: string }
        foreground_description:    { type: string }
        characters:                { type: array, items: { type: string } }
        objects:                   { type: array, items: { type: string } }
        text_overlay_suggestions:  { type: array, items: { type: string } }
        subtitle_timing:           { type: ["object", "null"] }
        sound_effect_suggestions:  { type: array, items: { type: string } }
        background_music_mood:     { type: string }
        b_roll_suggestions:        { type: array, items: { type: string } }
        stock_footage_suggestions: { type: array, items: { type: string } }
        asset_requirements:        { type: array, items: { type: object } }
        importance_score:          { type: number }
        estimated_image_count:     { type: integer }
        estimated_video_length_seconds: { type: number }

    StoryboardInput:
      type: object
      required: [topic]
      properties:
        scriptId:               { type: ["string", "null"] }
        researchId:             { type: ["string", "null"] }
        topic:                  { type: string, minLength: 3, maxLength: 500 }
        scriptStyle:
          type: string
          enum: [educational, documentary, storytelling, tutorial, news, long_form, shorts]
        scriptTone:
          type: string
          enum: [engaging, authoritative, casual, inspirational, conversational]
        targetDurationMinutes:  { type: integer, minimum: 1, maximum: 120 }
        targetAudience:         { type: string }
        language:               { type: string }
        providers:
          type: array
          items: { type: string }

    StoryboardResult:
      type: object
      required: [id, topic, status, providers, usedProviders, scenes, logs, createdAt, updatedAt]
      properties:
        id:                          { type: string }
        scriptId:                    { type: ["string", "null"] }
        researchId:                  { type: ["string", "null"] }
        topic:                       { type: string }
        title:                       { type: ["string", "null"] }
        status:
          type: string
          enum: [pending, running, completed, failed, cached]
        scriptStyle:                 { type: string }
        scriptTone:                  { type: string }
        targetDurationMinutes:       { type: integer }
        targetAudience:              { type: string }
        language:                    { type: string }
        version:                     { type: integer }
        scenes:
          type: array
          items: { $ref: "#/components/schemas/StoryboardScene" }
        sceneTimeline:
          type: array
          items: { type: object }
        narrationTiming:
          type: array
          items: { type: object }
        visualCues:
          type: array
          items: { type: object }
        totalDurationSeconds:         { type: ["integer", "null"] }
        sceneCount:                   { type: ["integer", "null"] }
        imageCount:                   { type: ["integer", "null"] }
        editingComplexityScore:       { type: ["number", "null"] }
        estimatedRenderTimeMinutes:   { type: ["integer", "null"] }
        estimatedCostUsd:             { type: ["number", "null"] }
        visualPacing:                 { type: ["string", "null"] }
        narrationPacing:              { type: ["string", "null"] }
        providers:
          type: array
          items: { type: string }
        usedProviders:
          type: array
          items: { type: string }
        jobId:          { type: ["string", "null"] }
        logs:
          type: array
          items: { type: string }
        errorMessage:   { type: ["string", "null"] }
        createdAt:      { type: string }
        updatedAt:      { type: string }
        completedAt:    { type: ["string", "null"] }

    StoryboardList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/StoryboardResult" }
        total: { type: integer }

    # ── Asset ────────────────────────────────────────────────────────────────
    AssetInput:
      type: object
      required: [storyboardId]
      properties:
        storyboardId:  { type: string }
        sceneIds:
          type: array
          items: { type: string }
          description: Empty = all scenes in the storyboard
        assetKinds:
          type: array
          items:
            type: string
            enum: [image, video, svg, chart, map, icon]
        providers:
          type: array
          items: { type: string }
          description: Ordered preference list — stock providers searched first, then generation
        forceGenerate: { type: boolean }

    AssetResult:
      type: object
      required: [id, storyboardId, sceneId, assetKind, status, license, tags, logs, createdAt, updatedAt]
      properties:
        id:                  { type: string }
        storyboardId:        { type: string }
        sceneId:             { type: string }
        assetKind:
          type: string
          enum: [image, video, svg, chart, map, icon]
        provider:            { type: ["string", "null"] }
        providerAssetId:     { type: ["string", "null"] }
        sourceUrl:           { type: ["string", "null"] }
        license:
          type: string
          enum: [public_domain, cc0, cc_by, cc_by_sa, commercial, generated, mit, unknown]
        prompt:              { type: ["string", "null"] }
        negativePrompt:      { type: ["string", "null"] }
        generationParameters:
          type: object
          additionalProperties: {}
        width:               { type: ["integer", "null"] }
        height:              { type: ["integer", "null"] }
        aspectRatio:         { type: ["string", "null"] }
        status:
          type: string
          enum: [pending, searching, downloading, generating, ready, failed, cached]
        costEstimateUsd:     { type: ["number", "null"] }
        generationTimeMs:    { type: ["integer", "null"] }
        fileSizeBytes:       { type: ["integer", "null"] }
        localCachePath:      { type: ["string", "null"] }
        thumbnailPath:       { type: ["string", "null"] }
        tags:
          type: array
          items: { type: string }
        qualityScore:        { type: ["number", "null"] }
        relevanceScore:      { type: ["number", "null"] }
        logs:
          type: array
          items: { type: string }
        errorMessage:        { type: ["string", "null"] }
        createdAt:           { type: string }
        updatedAt:           { type: string }
        completedAt:         { type: ["string", "null"] }

    AssetList:
      type: object
      required: [items, total]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AssetResult" }
        total: { type: integer }

    AssetAcquireResponse:
      type: object
      required: [items, total, message]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AssetResult" }
        total:   { type: integer }
        message: { type: string }

    AssetProviderStats:
      type: object
      required: [providerName, providerType, isEnabled, totalRequests, successfulRequests, failedRequests, cacheHits, totalCostUsd]
      properties:
        providerName:       { type: string }
        providerType:
          type: string
          enum: [stock, generate, icon]
        isEnabled:           { type: boolean }
        totalRequests:       { type: integer }
        successfulRequests:  { type: integer }
        failedRequests:      { type: integer }
        avgLatencyMs:        { type: ["number", "null"] }
        avgCostUsd:          { type: ["number", "null"] }
        totalCostUsd:        { type: number }
        cacheHits:           { type: integer }
        supportedKinds:
          type: array
          items: { type: string }

    AssetProviderStatsList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AssetProviderStats" }
